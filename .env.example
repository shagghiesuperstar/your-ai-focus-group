# Required — get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional — enables SSR scoring (arXiv:2510.08338 primary method).
# Without this, scoring falls back to FLR (LLM-as-rater).
# Get a key at https://platform.openai.com/api-keys
# Uses text-embedding-3-small (~$0.00002/1K tokens — negligible cost).
OPENAI_API_KEY=
